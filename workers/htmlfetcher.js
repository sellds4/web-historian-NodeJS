// eventually, you'll have some code here that uses the tested helpers
// to actually download the urls you want to download.

var readUrls = requires('readUrls');
var downloadUrls = requires('downloadUrls');